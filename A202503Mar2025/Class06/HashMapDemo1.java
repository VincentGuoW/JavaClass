package A202503Mar2025.Class06;

public class HashMapDemo1 {
    /*
     * HashMap part of map==> Use the same method
     * 1. Entry(key,value)==> Key one and only
     *      When input into HashMap,
     *      Only check the key, if key excist, new key replace old kay
     *      If non excist, new key hang below old key (jdk8+)
     *      If non excist, and alot, red black tree adjust by key.
     */
    public static void main(String[] args) {
        
    }
}
